# robot_navigation2/config/slam_template.yaml
/**:
  ros__parameters:
    base_frame: ${robot_name}/base_link
    odom_frame: ${robot_name}/odom
    map_frame:  ${robot_name}/map      # 带前缀
    scan_topic: scan
    resolution: 0.05
    max_laser_range: 6.0
    mode: mapping
